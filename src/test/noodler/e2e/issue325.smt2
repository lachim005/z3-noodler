(set-info :status sat)
(declare-const fucking_real_var Real)
(declare-const stupid_string_var String)
; (declare-const stupid_string_var2 String)
(assert (forall ((i Int)) (or (< i 0) (> i (to_int fucking_real_var)))))
(assert (str.< "a" stupid_string_var))
; (assert (<= (str.to_code ";") (str.to_code stupid_string_var)))
; (assert (> (str.to_int t) (to_int x)))
; (assert (< 1 (str.len stupid_string_var)))
; (declare-const z String)
; (assert (str.in_re z (re.++ (re.range "a" "z") re.all)))
; (assert (< (str.len z) (to_int fucking_real_var)))
(check-sat)
