(set-info :status sat)
(declare-const r Real)
(declare-const s String)
; (declare-const s2 String)
(assert (forall ((i Int)) (or (< i 0) (> i (to_int r)))))
(assert (str.< "a" s))
; (assert (<= (str.to_code ";") (str.to_code s)))
; (assert (> (str.to_int t) (to_int x)))
; (assert (< 1 (str.len s)))
; (declare-const z String)
; (assert (str.in_re z (re.++ (re.range "a" "z") re.all)))
; (assert (< (str.len z) (to_int r)))
(check-sat)
