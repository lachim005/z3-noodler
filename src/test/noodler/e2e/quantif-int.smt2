(set-logic ALL)
(set-info :status sat)
(declare-fun fun0 (String String Int) String)
(define-const c0 String ".")
(declare-const v0 String)
(assert (forall ((i Int))
    (>= (str.len (fun0 v0 "." i)) 1)))
(check-sat)
(get-model)